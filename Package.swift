import PackageDescription

let package = Package(
    name: "ReverseExtension",
    targets: [
        .target(
            name: "ReversExtension", 
            path: "ReverseExtension"
        )
    ],
)